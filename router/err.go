package router

const text_panic_rouet_nil = "router参数不能为nil"

const text_panic_router_register_twice = "不能在同一个服务下注册两个相同名字的路由,路由名: "

const text_panic_server_not_register = "该服务没有注册过路由"

const text_panic_server_init_twice = "不能初始化两次服务"
